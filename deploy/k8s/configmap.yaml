apiVersion: v1
kind: ConfigMap
metadata:
  name: supply-shock-config
  namespace: supply-shock
data:
  KAFKA_TOPIC_SIGNALS: "signals.raw"
  KAFKA_TOPIC_RISK: "risk.scored"
  ALERT_THRESHOLD: "72"
  ALERT_COOLDOWN_MINUTES: "30"
  SIMULATOR_TICK_SECONDS: "0"
  QUERY_API_HTTP_ADDR: ":8080"
  INGEST_HTTP_ADDR: ":8081"
