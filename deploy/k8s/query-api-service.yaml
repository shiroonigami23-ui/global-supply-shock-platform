apiVersion: v1
kind: Service
metadata:
  name: query-api
  namespace: supply-shock
spec:
  type: ClusterIP
  selector:
    app: query-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
